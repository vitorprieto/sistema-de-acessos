# Mark Lombok-generated methods with @lombok.Generated so JaCoCo excludes
# getters/setters/constructors from coverage — only hand-written logic counts.
lombok.addLombokGeneratedAnnotation = true
config.stopBubbling = true
